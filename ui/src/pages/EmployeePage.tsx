export function EmployeePage() {
  return (
    <div>
      <h1>Employee</h1>
      <p style={{ opacity: 0.7 }}>
        Claim-package upload + claim flow (Phase 2): build claim IT via the wallet plugin's
        encryption primitive, submit to PodClaimStore + the facade, then poll hasClaimed and
        the decrypted pToken balance.
      </p>
    </div>
  )
}
