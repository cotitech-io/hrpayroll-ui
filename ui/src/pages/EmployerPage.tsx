export function EmployerPage() {
  return (
    <div>
      <h1>Employer</h1>
      <p style={{ opacity: 0.7 }}>
        Campaign creation flow (Phase 3): roster entry, client-side merkle tree build, deploy +
        wire a new campaign facade, fund via encrypted pToken transfer + ackPoolCredit, and
        export per-employee claim packages.
      </p>
    </div>
  )
}
